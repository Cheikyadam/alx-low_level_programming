#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0
 */

int main(void)
{
long int a = 1;
long int b = 2;
int i;
long int c;

printf("1, ");
printf("2, ");
for (i = 3; i <= 50; i++)
{
c = a + b;
printf("%ld", c);
a = b;
b = c;
if (i != 50)
printf(", ");
}
printf("\n");
return (0);
}
