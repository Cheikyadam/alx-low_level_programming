#include "main.h"
#include <ctype.h>

/**
 * _isupper - to know if is upper
 * @c: the caracter to check
 *
 * Return: 1 or 0
 */

int _isupper(int c)
{
if (isupper(c))
return (1);
else
return (0);
}
