#include "main.h"

/**
 * _memset -setting memory
 * @s: the pointer
 * @b: the constant
 * @n: the number of bytes
 *
 * Return: char *
 */

char *_memset(char *s, char b, unsigned int n)
{
        return (s);
}
