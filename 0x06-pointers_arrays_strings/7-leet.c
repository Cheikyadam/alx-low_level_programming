#include "main.h"

/**
 * leet - coding 
 * @src: source
 *
 * Return: char *
 */

char *leet(char *src)
{
	int i = 0;

	for (i = 0; src[i] != '\0'; i++)
	{
		if (src[i] == 'a' || src[i] == 'A' || src[i] == 'e' ||
		src[i] == 'E' || src[i] == 'o' || src[i] == 'O'
		|| src[i] == 't' || src[i] == 'T' ||  src[i] == 'l'
		|| src[i] == 'L')
		{
		}
	}
	return (src);
}
